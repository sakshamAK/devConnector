import { extendTheme } from "@chakra-ui/react"

export const theme = extendTheme({
    colors: {
        brand: {
            pr: {
                500: "#17a2b8"
            },
            dark: "#343a40",
            light: "#f4f4f4",
            danger: "#dc3545",
            success: "#28a745"
        },
    }
})